interface:
  display_name: "Meeting Minutes"
  short_description: "Repo-specific patterns and workflows for meeting-minutes"
  default_prompt: "Use the meeting-minutes repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true